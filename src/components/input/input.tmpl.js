export const inputTmpl = `
    <label class="input">
        <div class="input__title">{{labelText}}</div>
        <input
            type="{{inputType}}"
            placeholder="{{inputPlaceholder}}"
            value="{{inputValue}}"
            name="{{inputName}}"
            required>
    </label>
`;
